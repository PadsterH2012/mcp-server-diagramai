# DiagramAI MCP Server Configuration

# DiagramAI API Configuration (Required)
DIAGRAMAI_API_URL=http://localhost:3000
DIAGRAMAI_API_KEY=your-api-key-here

# Optional: WebSocket URL (if different from API URL)
# DIAGRAMAI_WS_URL=ws://localhost:3000

# Optional: Request timeout in milliseconds (default: 30000)
# REQUEST_TIMEOUT=30000

# Optional: Enable debug logging (default: false)
# DEBUG=false

# Optional: Maximum retries for failed requests (default: 3)
# MAX_RETRIES=3

# Optional: Retry delay in milliseconds (default: 1000)
# RETRY_DELAY=1000

# Optional: Connection timeout in milliseconds (default: 10000)
# CONNECTION_TIMEOUT=10000

# Optional: User agent string for requests
# USER_AGENT=mcp-server-diagramai/1.0.0

# Optional: Rate limiting (requests per minute, default: 60)
# RATE_LIMIT_RPM=60

# Optional: Cache TTL in seconds (default: 300)
# CACHE_TTL=300
